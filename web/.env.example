# Base URL of the Jev Trader backend (no trailing slash).
# The app connects to $NEXT_PUBLIC_API_URL/events (SSE).
# Bot API. The paper script defaults the bot to 3100 and this dashboard to 3101.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3100
